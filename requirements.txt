# --- Python dependencies used by the DAGs ---
# psycopg2-binary and the Postgres/common-sql providers aren't listed here:
# the DAGs talk to model-registry entirely over HTTP (tasks/registry_client.py)
# and psycopg2-binary already ships in the base apache/airflow image for its
# own metadata-DB connection, so nothing needs to pull it in again.
#
# Unpinned: versions are fixed by the Airflow constraints file applied at
# build time (see Dockerfile), which matches them to the pinned Airflow core version.
requests
